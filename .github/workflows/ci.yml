name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python
        run: uv python install 3.13
      - name: Install
        run: uv sync --all-extras --dev
      - name: Ruff
        run: uv run ruff check src

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}
      - name: Build
        run: uv build
      - name: Smoke test
        run: |
          uv venv --python ${{ matrix.python-version }} .smoke
          .smoke/bin/python -m pip install dist/*.whl
          .smoke/bin/playmaker --help
      - name: Run unit tests
        run: .smoke/bin/python -m unittest discover -s tests -p "test_*.py" -v
